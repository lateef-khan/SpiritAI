# Commits listed here are pure reformatting. They carry no behaviour change, so
# `git blame` skips them and shows the commit that last changed the code itself.
#
# GitHub reads this file automatically. For local blame, run once:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs

# chore: adopt Prettier and format the web codebase
a970b77c458dbe7d3ae36046d3aa8a1b1240dc3d
