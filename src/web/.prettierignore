# Vite writes the bundle into the host's wwwroot; it is build output, not source.
../SpiritAI/wwwroot

# Lockfiles are generated, and reformatting one makes every future diff unreadable.
package-lock.json

# Written by OpenApiDocumentTests, byte for byte. Reformatting it here would make the next test
# run look like a change.
openapi/v1.json
