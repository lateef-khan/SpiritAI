# @openuidev/react-headless and @openuidev/react-ui peer zustand@^4.5.5 while the
# app (and @assistant-ui/*) runs zustand v5. The v5 tree satisfies them at
# runtime — deduped single copy, `npm ls` reports invalid only. Remove when
# upstream widens the peer range.
legacy-peer-deps=true
