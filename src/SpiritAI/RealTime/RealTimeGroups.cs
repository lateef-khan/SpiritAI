namespace SpiritAI.RealTime;

/// <summary>The groups the hub itself addresses. Features name their own.</summary>
public static class RealTimeGroups
{
    /// <summary>Every admitted socket, which is who hears how many of each kind are online.</summary>
    public const string Presence = "presence";
}
